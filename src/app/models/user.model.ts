export class User {
    
    constructor(
        public _id ?: Number,
        public username ?: String,
        public email ?: String,
        public password ? :String,
        public role ?: String,
        public image ? : String,
        public gethash ? : Boolean
    ){}

}